services:
  nextcloud:
    container_name: nextcloud
    image: nextcloud:stable
    ports:
      - 8080:80
    environment:
      MYSQL_HOST: nextcloud-db
      MYSQL_DATABASE: nextcloud
      MYSQL_USER: nextcloud
      MYSQL_PASSWORD: ${NEXTCLOUD_DB_PASSWORD}
      NEXTCLOUD_ADMIN_USER: ${NEXTCLOUD_ADMIN_USER}
      NEXTCLOUD_ADMIN_PASSWORD: ${NEXTCLOUD_ADMIN_PASSWORD}
      NEXTCLOUD_TRUSTED_DOMAINS: "localhost:8080"
      REDIS_HOST: nextcloud-redis
    volumes:
      - ./volumes/nextcloud:/var/www/html
    depends_on:
      - nextcloud-db
      - nextcloud-redis    
    labels:
      kuma.nextcloud.http.name: "Nextcloud"
      kuma.nextcloud.http.url: "http://nextcloud:80"
    restart: always

  nextcloud-db:
    container_name: nextcloud-db
    image: mariadb:lts
    command: --transaction-isolation=READ-COMMITTED --binlog-format=ROW
    environment:
      MYSQL_ROOT_PASSWORD: ${NEXTCLOUD_DB_ROOT_PASSWORD}
      MYSQL_DATABASE: nextcloud
      MYSQL_USER: nextcloud
      MYSQL_PASSWORD: ${NEXTCLOUD_DB_PASSWORD}
    volumes:
      - ./volumes/nextcloud-db:/var/lib/mysql
    restart: always

  nextcloud-redis:
    container_name: nextcloud-redis
    image: redis:alpine
    volumes:
      - ./volumes/nextcloud-redis:/data
    restart: always

  # syncthing:
  #   container_name: syncthing
  #   image: lscr.io/linuxserver/syncthing:latest
  #   labels:
  #     kuma.syncthing.http.name: "Syncthing"
  #     kuma.syncthing.http.url: "http://syncthing:8384"
  #   hostname: homebase-sync
  #   environment:
  #     PUID: 1000
  #     PGID: 1000
  #     TZ: America/Detroit
  #   ports:
  #     - 8384:8384
  #     - 22000:22000/tcp
  #     - 22000:22000/udp
  #     - 21027:21027/udp
  #   volumes:
  #     - ./config/syncthing:/config
  #     - ./volumes/syncthing:/data
  #   restart: always

networks:
  default:
    name: homebase
    external: true
